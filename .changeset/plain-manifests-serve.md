---
"@next-community/adapter-vercel": patch
---

Serve a plain text 404 for subresource requests (images, fonts, manifests, scripts, etc.) to unknown paths, instead of invoking the app's not-found output.
